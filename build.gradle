apply plugin: 'java'
apply plugin: 'groovy'

apply plugin: 'idea'
apply plugin: 'eclipse'

apply plugin: 'wrapper'

dependencies {
	compile gradleApi()
	compile localGroovy()
}

sourceSets {
	main {
		groovy {
			srcDir 'src/main/groovy'
		}
	}
}

